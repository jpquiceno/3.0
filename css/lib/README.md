CSS files included with libriares used in the template, which are NOT compressed by the build script, but may be compressed on their own. 

MediaElement images are in this folder, and should be moved to ../img

